# 0.1 Release Publication Draft

## Purpose

Hold the current internal draft for the `0.1` GitHub release body and the manual publication order, without embedding that working text directly into the `M7` worklist.

## GitHub Release Body Draft

```md
TermiWeb 0.1.0 is the first public `0.1` release.

TermiWeb is a browser-first shared terminal for a Windows host. It keeps the terminal in the browser so you can stay attached from the host or another device without remoting the whole desktop.

## What it supports

- Shared live terminal instances in the browser
- Cross-device attachment to the same live shell
- A packaged Windows run surface with setup, start, restart, stop, uninstall, and optional before-sign-in auto-start
- Mobile-oriented controls, selection/copy support, and a fallback paste field when direct browser paste is blocked

## Packaged quick start

1. Download `TermiWeb-0.1.0-windows-x64.zip`.
2. Unpack it into a user-writable folder.
3. Open `1.Start-Here.md` and read `DISCLAIMER.md`.
4. Run `Set Up TermiWeb.cmd`.
5. Approve the Windows elevation prompt when TermiWeb starts.
6. Open `http://127.0.0.1:22443` if setup does not open it for you.

## Operating boundaries

- Windows hosts only
- Every shell in `0.1` is elevated
- Built for a trusted private network
- WAN exposure is possible, but securing it is up to you
- Includes single-user auth without TLS to keep it simple

## Known issues

- [Collapsed mobile instance controls are too small to use cleanly](https://github.com/n8bar/TermiWeb/issues/2)
- [Mobile keyboard text assistance can corrupt terminal input](https://github.com/n8bar/TermiWeb/issues/3)

## Links

- Download: `https://termiweb.com/download/`
- Download site: `https://termiweb.com`
- Source repository: `https://github.com/n8bar/TermiWeb`
- README: `https://github.com/n8bar/TermiWeb/blob/main/README.md`
- Disclaimer: `https://github.com/n8bar/TermiWeb/blob/main/DISCLAIMER.md`
```

## Manual Publication Order

1. Confirm `main` contains the final `M7` release surface.
2. Run `npm run typecheck`, `npm test`, `npm run build`, `npm run build:site`, and `npm run package:release`.
3. Verify the local outputs, especially `artifacts/release/TermiWeb-0.1.0-windows-x64.zip` and `dist/site/`.
4. Draft the GitHub release titled `TermiWeb 0.1.0` and upload `TermiWeb-0.1.0-windows-x64.zip`.
5. Wire the site and README to the canonical download path backed by the current release artifact.
6. Publish the stable `milestones.ics` URL.
7. Push the final release-surface updates to `main`.
8. Push `main` and let the GitHub Pages workflow deploy the site.
9. Configure the root custom domain, apex DNS records, `www` redirect behavior, and Pages HTTPS state.
10. Publish the GitHub release.
11. Verify the live README, release, website, domain, and milestone-feed URLs together.
